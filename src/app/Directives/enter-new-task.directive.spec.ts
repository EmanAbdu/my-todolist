import { EnterNewTaskDirective } from './enter-new-task.directive';

describe('EnterNewTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterNewTaskDirective();
    expect(directive).toBeTruthy();
  });
});
