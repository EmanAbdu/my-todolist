import { EnterNewTodaytaskDirective } from './enter-new-todaytask.directive';

describe('EnterNewTodaytaskDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterNewTodaytaskDirective();
    expect(directive).toBeTruthy();
  });
});
